import React from "react";
import s from "./avatarmenu.module.scss";

const AvatarMenu = ({ isOpen }) => {
  return <div onClick={() => {}} className={s.avatarMenu}></div>;
};

export default AvatarMenu;
